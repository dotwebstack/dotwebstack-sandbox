graph config:TestStage {
  
  config:CharacterResource a elmo:Representation;
    elmo:appliesTo [rdf:type lotro:Wizard];
    elmo:index "1";
    elmo:contains config:HeaderAppearance;
    elmo:contains config:NavbarSearchAppearance;
    elmo:contains config:TableCharacterResource;
  .
  
  config:TableCharacterResource a elmo:Representation;
    elmo:informationProduct config:IP_CharacterResource;
  .
}